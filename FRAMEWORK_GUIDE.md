# 三层博客框架使用指南

## 📐 框架结构

本博客采用三层结构，帮助读者快速找到内容，也帮助你更好地组织和管理文章。

```
层 1：文章（Posts）
  ↓
层 2：主题索引（Start Here）
  ↓
层 3：归档与检索（Tags/Categories/Archive）
```

---

## 层 1：文章（Posts）

### 位置
`_posts/YYYY-MM-DD-title.md`

### Front Matter 模板

每篇文章必须使用统一的 Front Matter 格式：

```yaml
---
title: "文章标题"
date: YYYY-MM-DD
tags: [tag1, tag2, tag3]
categories: [分类名]
excerpt: "文章摘要，50-160字，简洁概括核心内容"
---
```

### 字段说明

| 字段 | 必需 | 说明 | 示例 |
|------|------|------|------|
| `title` | ✅ | 文章标题 | `"Python 装饰器详解"` |
| `date` | ✅ | 发布日期，格式 YYYY-MM-DD | `2026-01-27` |
| `tags` | ⚠️ | 标签数组，细粒度检索 | `[Python, 装饰器, 高级特性]` |
| `categories` | ⚠️ | 分类数组，大主题分区 | `[技术]` |
| `excerpt` | ❌ | 文章摘要，用于列表页显示 | `"深入解析 Python 装饰器的原理与应用"` |

### 标签 vs 分类

**标签（Tags）**：细粒度检索
- 使用具体的技术、工具、概念名称
- 示例：`tauri`, `career`, `btc`, `Python`, `Django`, `RESTful API`

**分类（Categories）**：大主题分区
- 使用大主题分类
- 建议分类：`技术`, `职场`, `思考`, `工具`, `生活`

### 创建新文章步骤

1. **复制模板**：复制 `_posts/TEMPLATE.md`
2. **重命名**：`YYYY-MM-DD-文章标题.md`
3. **填写 Front Matter**：按照模板填写所有字段
4. **编写内容**：使用 Markdown 编写文章
5. **提交发布**：`git add`, `git commit`, `git push`

---

## 层 2：主题索引（Start Here）

### 位置
`start.md` - 访问路径：`/start/`

### 作用
这是**长期写作者最强的"护城河"**：
- ✅ 读者不会迷路：清晰的导航和主题组织
- ✅ 你自己也不迷路：系统化的内容管理

### 结构

#### 1. 入门路线
按主题组织，帮助新读者快速入门：
- 技术入门
- 职场成长
- 思考与复盘

#### 2. 精华清单
展示最重要的文章：
- 必读文章
- 热门文章

#### 3. 系列文章
将相关文章串联成系列：
- 系列一：技术栈深入
- 系列二：职场成长
- 系列三：思考与复盘

### 维护建议

**每次发布新文章时**：
1. 如果是系列文章，更新对应的系列列表
2. 如果是重要文章，考虑加入"精华清单"
3. 如果是入门文章，更新"入门路线"

**定期维护**：
- 每月检查一次，确保链接正确
- 根据文章质量调整"精华清单"
- 完善系列文章的连贯性

---

## 层 3：归档与检索

### 3.1 标签页（Tags）

**位置**：`tags.md` - 访问路径：`/tags/`

**作用**：细粒度检索，按标签查找文章

**使用场景**：
- 查找特定技术：如 `Python`, `React`, `Docker`
- 查找特定主题：如 `career`, `btc`, `tauri`
- 查找系列标签：如 `系列-技术栈`

**维护**：
- 标签会自动从文章的 `tags` 字段生成
- 建议使用统一的标签命名规范
- 避免创建过多相似标签

### 3.2 分类页（Categories）

**位置**：`categories.md` - 访问路径：`/categories/`

**作用**：大主题分区，按分类浏览文章

**使用场景**：
- 浏览某个大主题的所有文章
- 如：查看所有"技术"类文章

**维护**：
- 分类会自动从文章的 `categories` 字段生成
- 建议使用固定的几个大分类
- 避免创建过多分类

### 3.3 归档页（Archive）

**位置**：`archive.md` - 访问路径：`/archive/`

**作用**：按时间顺序浏览所有文章

**使用场景**：
- 查看历史文章
- 按时间线了解博客发展
- 发现早期文章

**维护**：
- 自动按日期排序
- 无需手动维护

---

## 🎯 使用流程

### 对于读者

1. **新读者**：
   - 访问 `/start/` → 查看"入门路线"
   - 按顺序阅读推荐文章

2. **寻找特定内容**：
   - 使用 `/tags/` 按标签查找
   - 使用 `/categories/` 按分类浏览

3. **探索发现**：
   - 浏览 `/archive/` 发现历史文章
   - 查看 `/start/` 的"系列文章"

### 对于作者

1. **发布新文章**：
   ```
   创建文章 → 填写 Front Matter → 提交发布
   ```

2. **维护主题索引**：
   ```
   发布后 → 更新 start.md → 添加到相应主题
   ```

3. **组织系列文章**：
   ```
   规划系列 → 发布文章 → 在 start.md 中串联
   ```

---

## 📋 最佳实践

### ✅ 推荐做法

1. **统一的 Front Matter**：
   - 始终使用模板格式
   - 填写完整的 tags 和 categories
   - 为重要文章添加 excerpt

2. **合理的标签体系**：
   - 使用具体的技术名称
   - 避免过于宽泛的标签
   - 保持标签命名一致性

3. **清晰的分类体系**：
   - 使用固定的大分类
   - 避免创建过多分类
   - 每个分类有明确主题

4. **及时更新主题索引**：
   - 发布后立即更新 start.md
   - 定期检查链接有效性
   - 完善系列文章连贯性

### ❌ 避免

1. 不要跳过 Front Matter 字段
2. 不要创建过多相似标签
3. 不要忘记更新 start.md
4. 不要使用过于宽泛的分类

---

## 🔧 文件清单

### 核心文件
- `_posts/TEMPLATE.md` - 文章模板
- `start.md` - 主题索引页
- `tags.md` - 标签页
- `categories.md` - 分类页
- `archive.md` - 归档页

### 配置文件
- `_config.yml` - 已更新导航菜单

---

## 📚 相关文档

- [Jekyll + Minimal Mistakes 使用指南](JEKYLL_GUIDE.md)
- [文章模板](_posts/TEMPLATE.md)

---

**提示**：这个三层框架是长期写作者的核心竞争力，花时间维护它，会让你的博客更有价值！
